name: Reproduce DeepGHS Metadata Error

on:
  workflow_dispatch:

jobs:
  repro:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      HF_ENDPOINT: https://hub.deepghs.org
      PYTHONUNBUFFERED: "1"
      HF_HUB_DISABLE_PROGRESS_BARS: "1"
      REPRO_MODE: download
      REPRO_TARGET_REPO: deepghs/reddit_mostlyhumans_index
      REPRO_TARGET_FILE: mostlyhumans.csv
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install huggingface_hub==0.36.0 requests==2.32.5

      - name: Run reproduction script
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: python deepghs_metadata_repro.py
